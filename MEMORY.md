# MEMORY.md

## 项目信息

- 项目名：AcoustiCore
- 项目路径：`D:\Workspace\AcoustiCore`
- 创建/整理日期：2026-06-30
- 当前状态：已恢复为可运行的纯静态网页，产品名为“AcoustiCore 影院配置助手”。
- 项目方向：面向普通客户的影院销售配置演示工具；销售模式用于展开数据依据，不增加客户操作负担。

## 项目约定

- 进入项目后先读取 `AGENTS.md` 和 `MEMORY.md`。
- 不把临时脚本、测试输出和渲染文件散落在项目根目录。
- 后续代码建议放入 `src/`，文档放入 `docs/`，素材放入 `assets/`，输出放入 `outputs/`。
- 生成、修改、修复任何可交付文件后，必须做本地实际校验。
- 声学、影音、硬件和安装结论必须区分经验判断、参数计算和现场测量验证。

## 已知历史方向

- AcoustiCore Planner 目标是家庭影院 / 声学规划桌面软件原型。
- 历史记忆中曾记录第一阶段技术方向为 PySide6 + SQLite。
- 历史记忆中曾记录房间与座位模块、MLP 拖动、参考线、扬声器角度线、低音炮候选点等规划功能。
- 当前目录为空，以上内容仅作为历史方向参考；真正恢复开发前，需要重新确认代码、依赖或旧项目文件位置。

## 后续优先检查

- 是否存在旧项目目录、备份或迁移源。
- 是否需要恢复 PySide6 + SQLite 原型。
- 是否需要重建项目结构：`src/`、`docs/`、`assets/`、`outputs/`。
- 是否需要补充 README.md、启动命令、依赖清单和验证命令。

## 本次整理记录

- 2026-06-30：补齐项目级 `AGENTS.md` / `MEMORY.md`，以当前真实目录 `D:\Workspace\AcoustiCore` 作为执行入口。
- 2026-07-24：新增 `docs/soundbox_acoustic_knowledge_guide.md`，沉淀影院/听音室设计逻辑、Eyring/Sabine RT 计算、产品数据模型、检测证据和交付规则。

## 声学知识库规则

- Vicoustic 白皮书和工作簿只能作为方法论及竞品数据来源，不能直接作为声博士（Soundbox）产品性能声明。
- 产品数据库必须区分吸声系数 `alpha` 与每件等效吸声面积 `Aeq`，同时记录测试标准、实验室、样品构造、报告编号和版本。
- 统计 RT 模型不替代早期反射、低频模态、多座位一致性、隔声、HVAC 噪声和现场测量。
- 方案输出必须标记经验建议、参数计算、设备限制与待测量确认四类结论。
- 声博士知识储备的当前主文档为 `docs/soundbox_acoustic_knowledge_guide.md`。

## 影院配置助手产品与数据规则

- 主流程固定为“选择体验 → 输入空间与配置 → 查看差距与升级效果”，默认客户视图，数据依据按需展开。
- 三档目标为日常电影 `95/105 dB`、沉浸影院 `100/110 dB`、高动态影院 `105/115 dB`；均指系统峰值能力，前两档是产品预设。
- 主声道只用灵敏度、可用功率与距离计算；`2.83V/1m` 必须结合声明阻抗换算，持续功率与规格参考分别使用默认 `±3 dB` 和 `±5 dB` 规划范围。
- 不再使用固定房间衰减、分频奖励、4 Ω 自动功率倍增或绝对“达标”评分；结果使用整数与范围表达。
- V2.5 低音炮数据生成 2,032 个规范化型号和 93 个多实测型号复核项；大小写不同的 `PSA S3600i` 键合并为同一型号。
- 客户视图只能使用 `primaryTestId`；配置差异未确认时不设默认主记录。销售模式切换替代测试不改写目录，CEA-2010-A 与 CTA-2010-B 禁止混排。
- 无实测型号和本机自定义型号只展示规格；自定义数据使用浏览器本机存储及 JSON 导入导出，不自动写入公共目录。
- 多炮容量按保守能量叠加，每翻倍约 `+3 dB`；房间尺寸只作风险说明，不生成固定房间增益。最终结果必须现场测量。

## 常用命令

- Windows 双击启动：`启动本地预览.cmd`（自动寻找系统 Node 或 Codex 自带 Node，启动服务并打开浏览器）
- 单文件交付：运行 `npm run build:single` 生成 `dist/AcoustiCore-影院配置助手.html`；该文件内嵌样式、逻辑和版本化低音炮目录，可离线双击使用。
- 本地运行：`npm run dev -- --host 127.0.0.1 --port 4173`
- 自动测试：`npm test`
- 重建低音炮目录：`npm run data:build -- "源 data.js 完整路径"`

## 本次实现记录

- 2026-08-12：将远端单文件页面重构为静态模块化“影院配置助手”，接入 V2.5 低音炮目录、客户/销售双视图、本机自定义型号、一页打印报告及自动测试。
- 2026-08-19：升级为 v3.1.0，证据等级固定为 `verified_measurement`、`traceable_measurement`、`manufacturer_spec`、`legacy_proxy`，结论固定为 `screening_pass`、`screening_shortfall`、`provisional`、`insufficient`。
- 只有来源链接、许可、人工审核、测试口径和六频点完整的记录才能产生初筛通过/缺口；历史导入数据默认只产生暂定结果。
- `legacy-size-power-v1` 旧规格公式允许保留单一 dB，但永久为 `legacy_proxy / provisional`，不得生成达标或认证结论。
- 93 个多实测型号全部取消自动客户主记录；CEA-2010-A、CTA-2010-B、CTA-2010-C 必须分组。
- 新增本机音箱/功放库、CSV/JSON 导入、XLSX 模板、`ProjectFileV2` 项目导入导出、轴向模态提示、多座位一致性判断和报告追溯。
- v3.1.0 验证基线：`node --test --test-isolation=none tests/*.test.mjs` 共 28 项通过；模块版与单文件版均通过 1440px/390px Chrome 端到端验收，390px 无横向溢出。
