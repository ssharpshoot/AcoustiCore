# AcoustiCore 设计与验收记录

状态：**PASS**  
验收日期：2026-08-19（v3.1.0）

## 视觉与信息架构

- 保留原版深色、青色强调的专业视觉识别，同时将主流程收敛为“选择体验 → 输入空间与配置 → 查看差距与升级”。
- 默认结果区只呈现当前能力、差距原因和可执行调整；公式、测试口径及替代记录收进“查看数据依据”。
- 结果使用整数及规划范围，未发现 0.1 dB 虚假精度、S/A/B 评分或官方达标承诺。
- 低频分为输出容量和多座位一致性；六个 CEA/CTA 频点均可见。
- 桌面结果区保持可见，平板和手机改为顺序阅读；手机步骤导航已从横向滚动改为紧凑三列。

## 交互验收

- 三档体验目标均参与计算；悬停、键盘聚焦和手机点击可显示释义。
- 数字框和滑杆双向同步，边界值自动限制；滑杆控件具有 44 px 操作高度。
- 低音炮数量步进按钮及 1/2/4 只快捷按钮可用，多炮前后容量差异和客户解释实时更新。
- 销售模式可切换同型号替代测试；收起销售模式后恢复客户主记录，未改写目录默认值。
- 无实测型号显示规格和数据缺口，不生成绝对容量；本机自定义型号保存、导入、导出路径可用。
- 低音炮搜索支持方向键、回车与 `aria-activedescendant`；项目 JSON 导出后可重新导入恢复。
- 本机音箱/功放库支持 CSV/JSON，客户模式只显示已审核记录。

## 数据保护验收

- V2.5 源数据 2,224 条记录规范化为 2,032 个型号。
- 93 个多实测型号已生成复核清单，全部不自动设置客户主记录。
- CEA-2010-A、CTA-2010-B 与 CTA-2010-C 使用独立标准标识，不进入同一比较集合。
- 多炮仅使用同一条主记录的多频点数据，并按保守能量叠加估算；不添加固定房间增益。
- `legacy-size-power-v1` 无论数值多高都只能输出 `provisional`。

## 尺寸与打印

| 尺寸 | 横向溢出 | 控件可操作 | 结果 |
| --- | --- | --- | --- |
| 1440 × 900 | 无 | 是 | PASS |
| 768 × 1024 | 无 | 是 | PASS |
| 390 × 844 | 无（含内部组件） | 是 | PASS |

- 打印媒体已实际渲染复核；报告包含项目、版本、计算模型、设备、六频点/旧公式状态、证据等级、假设、风险、验证项和联系二维码。
- 桌面、手机和打印视图无文字截断、重叠或不可读内容。

## 自动测试

- `node --test --test-isolation=none tests/*.test.mjs`：28 项通过，0 项失败。
- 覆盖证据等级、结论状态、旧公式锁定、设备功率上限、CTA/CEA 隔离、93 条人工复核、项目文件、设备 CSV、自定义型号和滑杆同步。

## 单 HTML 交付

- `npm run build:single` 生成 `dist/AcoustiCore-影院配置助手.html`，当前约 1.37 MB，并内嵌联系二维码。
- CSS、计算模块、交互逻辑及 V2.5 低音炮目录均已内嵌；页面不存在外部样式表、外部脚本或 ES module 依赖。
- 内嵌脚本已通过 classic script 语法检查；浏览器加载无警告/错误。
- 1440 × 900 与 390 × 844 实测无横向溢出，视觉层级与模块版一致。
- 已验证设备库应用、项目导出/重载、键盘搜索、实测记录暂定状态、旧规格公式暂定状态和二维码加载。
- 若某浏览器限制 `file://` 本机存储，自定义型号仍可在当前会话使用并导出 JSON，不会阻断添加流程。

## 后续数据工作

- 93 个多记录型号需要逐条人工确认端口、DSP、供电、箱体或驱动版本。
- 475 条历史测试记录仍需补具体来源链接、许可状态、测试日期和审核人；完成前只能显示暂定结果。
