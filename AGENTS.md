# AGENTS.md instructions for D:\Workspace\AcoustiCore

## 1. 项目定位

本项目用于 AcoustiCore Planner 家庭影院 / 声学规划桌面软件原型。

默认目标不是做演示页面，而是做可运行、可验证、能服务家庭影院方案设计和销售展示的专业工具。

## 2. 工作方式

- 默认用中文协作。
- 代码、命令、路径、变量名和文件名保持英文。
- 结论先行，先给判断，再给原因和执行结果。
- 修改代码、配置或文档前，先读取本项目 `AGENTS.md` 和 `MEMORY.md`。
- 优先做能完整解决需求的最小改动，不顺手重构无关代码。
- 生成或修改文件后，必须做本地实际校验，再告知结果。

## 3. 产品与交互规则

- 以家庭影院、客厅影院、影音室方案规划和声学校准工作流为核心。
- 优先支持可视化、实时反馈、参数调节、前后对比和引导式流程。
- 普通客户要能看懂方案价值，专业用户要能看到关键参数和验证依据。
- UI 设计要服务销售展示和交付维护，不做只好看的静态概念稿。

## 4. 声学与影音规则

涉及房间、座位、扬声器、低音炮、DSP、EQ、延迟、相位和校准建议时，必须区分：

- 经验建议
- 参数计算
- 设备能力限制
- 需要现场测量确认的结论

不能把未经房间尺寸、设备参数或现场测量验证的判断说成绝对事实。

## 5. 验证要求

- 桌面软件改动后，优先运行可用的本地启动或测试命令。
- 涉及界面时，检查关键视图是否能打开、控件是否可操作、文本是否溢出。
- 涉及声学计算时，检查输入边界、单位、公式来源和输出解释。
- 涉及保存文件或数据库时，检查文件可写、可读、数据能重新加载。

## 6. 记忆回写

如果本项目中形成新的稳定规则，写回 `MEMORY.md`，例如：

- 架构决策
- UI / 交互规则
- 声学计算规则
- 已验证命令
- 数据库结构
- 用户纠正过的产品或交付标准

会话结束前，用一句话说明本次是否新增了 `MEMORY.md` 内容。
